name: Semantic PR Title

on: [pull_request]

env:
  THING_I_NEED: ${{ secrets.MY_SUPER_SECRET }}

jobs:
  script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: .github/scripts/script.js