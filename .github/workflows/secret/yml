name: get secret

on: push

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Secret
        run: echo "hello"
        run: | 
          echo "secret is: ${{ secrets.MY_SUPER_SECRET }}"
